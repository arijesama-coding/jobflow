import { Component } from '@angular/core';

@Component({
  selector: 'app-tasks',
  standalone: true,
  template: `
    <div class="page">
      <h1>Tasks</h1>
      <p>Tasks module — to be implemented (Phase per README roadmap).</p>
    </div>
  `,
})
export class TasksComponent {}
