import { Component } from '@angular/core';

@Component({
  selector: 'app-jobs',
  standalone: true,
  template: `
    <div class="page">
      <h1>Jobs</h1>
      <p>Jobs module — to be implemented (Phase per README roadmap).</p>
    </div>
  `,
})
export class JobsComponent {}
