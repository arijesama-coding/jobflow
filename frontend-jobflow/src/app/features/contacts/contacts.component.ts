import { Component } from '@angular/core';

@Component({
  selector: 'app-contacts',
  standalone: true,
  template: `
    <div class="page">
      <h1>Contacts</h1>
      <p>Contacts module — to be implemented (Phase per README roadmap).</p>
    </div>
  `,
})
export class ContactsComponent {}
