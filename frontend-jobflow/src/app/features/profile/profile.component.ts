import { Component } from '@angular/core';

@Component({
  selector: 'app-profile',
  standalone: true,
  template: `
    <div class="page">
      <h1>Profile</h1>
      <p>Profile module — to be implemented (Phase per README roadmap).</p>
    </div>
  `,
})
export class ProfileComponent {}
