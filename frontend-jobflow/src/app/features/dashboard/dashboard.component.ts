import { Component } from '@angular/core';

@Component({
  selector: 'app-dashboard',
  standalone: true,
  template: `
    <div class="page">
      <h1>Dashboard</h1>
      <p>Dashboard module — to be implemented (Phase per README roadmap).</p>
    </div>
  `,
})
export class DashboardComponent {}
